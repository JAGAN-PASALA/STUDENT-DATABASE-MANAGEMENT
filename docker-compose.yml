version: '3.8'

services:
  web:
    build: .
    container_name: student_db_portal
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persists SQLite database between container restarts
      - sqlite_data:/app
    restart: unless-stopped

volumes:
  sqlite_data: